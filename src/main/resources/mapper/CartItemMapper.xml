<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qbk.web.mapper.CartItemMapper">
    <resultMap id="CartItemResultMap" type="com.qbk.entity.CartItem">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="cart_id" property="cartId" jdbcType="BIGINT" />
        <result column="product_id" property="productId" jdbcType="BIGINT" />
        <result column="product_name" property="productName" jdbcType="VARCHAR" />
        <result column="price" property="price" jdbcType="DOUBLE" />
        <result column="quantity" property="quantity" jdbcType="INTEGER" />
        <result column="stock" property="stock" jdbcType="INTEGER" />
        <result column="image" property="image" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <select id="findByCartId" resultMap="CartItemResultMap">
        SELECT * FROM cart_item WHERE cart_id = #{cartId}
    </select>

    <select id="findByCartIdAndProductId" resultMap="CartItemResultMap">
        SELECT * FROM cart_item WHERE cart_id = #{cartId} AND product_id = #{productId}
    </select>

    <insert id="insert" parameterType="com.qbk.entity.CartItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_item (cart_id, product_id, product_name, price, quantity, stock, image, create_time, update_time)
        VALUES (#{cartId}, #{productId}, #{productName}, #{price}, #{quantity}, #{stock}, #{image}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.qbk.entity.CartItem">
        UPDATE cart_item
        <set>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="price != null">price = #{price},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="image != null">image = #{image},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM cart_item WHERE id = #{id}
    </delete>

    <delete id="deleteByCartId">
        DELETE FROM cart_item WHERE cart_id = #{cartId}
    </delete>
</mapper>